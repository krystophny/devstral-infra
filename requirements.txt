# vLLM: unified inference engine across all platforms
# macOS: installed via vllm-metal curl installer (setup_mac.sh)
# Linux/WSL CUDA: pip install vllm>=0.8.0
# Linux CPU: pip install vllm>=0.8.0 --extra-index-url pytorch/cpu
vllm>=0.8.0
